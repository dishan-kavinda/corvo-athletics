import { SplitChooser } from '@/components/ui/SplitChooser';

export const metadata = {
  title: 'Corvo Athletic — Choose Your Aesthetic',
  robots: { index: false, follow: false },
};

export default function ChooserPage() {
  return <SplitChooser />;
}
